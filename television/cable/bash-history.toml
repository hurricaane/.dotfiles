[metadata]
name = "bash-history"
description = "A channel to select from your bash history"
requirements = ["bash"]

[source]
command = "sed '1!G;h;$!d' (set -q HISTFILE; and echo $HISTFILE; or echo $HOME/.bash_history)"
no_sort = true
frecency = false
